import { useState } from "react";
import { Link, useNavigate } from "react-router-dom";
import { ArrowRight, BriefcaseBusiness, Lock, Mail, User } from "lucide-react";
import AuthLayout from "@/components/layouts/AuthLayout";
import Logo from "@/components/ui/Logo";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { registerUser } from "@/services/authService";

function Register() { const navigate = useNavigate(); const [formData, setFormData] = useState({ full_name: "", email: "", password: "" }); const [loading, setLoading] = useState(false); const [error, setError] = useState(""); const [success, setSuccess] = useState(false); const update = (event) => setFormData((current) => ({ ...current, [event.target.name]: event.target.value })); const submit = async (event) => { event.preventDefault(); setError(""); setLoading(true); try { await registerUser(formData); setSuccess(true); } catch (err) { setError(err.response?.data?.detail || "We couldn’t create your account. Please try again."); } finally { setLoading(false); } }; return <AuthLayout><div className="relative w-full max-w-md overflow-hidden rounded-2xl border border-slate-700/80 bg-slate-900/75 p-6 shadow-2xl shadow-black/30 backdrop-blur-xl sm:p-8"><div className="absolute -right-14 -top-14 h-32 w-32 rounded-full bg-cyan-400/10 blur-3xl" /><div className="relative"><Logo /><div className="mt-8"><span className="inline-flex items-center gap-1.5 rounded-full border border-cyan-400/20 bg-cyan-400/10 px-3 py-1 text-xs font-medium text-cyan-200"><BriefcaseBusiness size={13} />Set up your workspace</span><h1 className="mt-4 text-3xl font-semibold tracking-tight text-white">Start hiring with clarity.</h1><p className="mt-2 text-sm leading-6 text-slate-400">Create your KhichiSphere account and bring your hiring workflow into one place.</p></div>{success ? <div className="mt-7 rounded-xl border border-emerald-400/20 bg-emerald-400/10 p-4"><p className="font-medium text-emerald-300">Account created successfully.</p><button onClick={() => navigate("/login")} className="mt-2 inline-flex items-center gap-1 text-sm font-medium text-emerald-200">Continue to sign in <ArrowRight size={15} /></button></div> : <form onSubmit={submit} className="mt-7 space-y-4">{[["full_name", "Full name", "Your name", User, "text"], ["email", "Work email", "you@company.com", Mail, "email"], ["password", "Password", "At least 8 characters", Lock, "password"]].map(([name, label, placeholder, Icon, type]) => <div key={name} className="space-y-1.5"><Label htmlFor={name} className="text-sm font-medium text-slate-300">{label}</Label><div className="relative"><Icon size={17} className="absolute left-3.5 top-1/2 -translate-y-1/2 text-slate-500" /><Input id={name} name={name} type={type} value={formData[name]} onChange={update} placeholder={placeholder} required minLength={name === "password" ? 8 : undefined} className="h-11 rounded-lg border-slate-700 bg-slate-950/40 pl-10 text-slate-100 placeholder:text-slate-600 focus-visible:border-cyan-400/50" /></div></div>)}{error && <p className="rounded-lg bg-red-500/10 p-3 text-sm text-red-300">{error}</p>}<Button type="submit" disabled={loading} className="mt-2 h-11 w-full rounded-lg bg-cyan-400 font-semibold text-slate-950 hover:bg-cyan-300">{loading ? "Creating account..." : "Create account"}<ArrowRight size={16} /></Button></form>}<p className="mt-6 text-center text-sm text-slate-400">Already have an account? <Link to="/login" className="font-medium text-cyan-300 hover:text-cyan-200">Sign in</Link></p></div></div></AuthLayout>; }
export default Register;
